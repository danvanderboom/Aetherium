# Aetherium Subsystem Audits

*Audit date: 2026-07-03. Conducted per the quality-improvement effort; see [RECOMMENDATIONS.md](RECOMMENDATIONS.md) for the consolidated findings register, [DESIGN_ANALYSIS.md](DESIGN_ANALYSIS.md) for the system-design assessment, and [IMPROVEMENT_PLAN.md](IMPROVEMENT_PLAN.md) for the roadmap.*

Findings in each audit are marked **Verified** (confirmed against code, build output, or test runs with `file:line` evidence) or **Suspected** (plausible from reading, not independently confirmed).

> **Reconciliation note (`develop` @ 2026-07-03, updated after Phase 4).** The ten audits below were written against baseline `5b7e267`. Since then: 23 commits landed on `develop` (reconciled in `d01fb94`), followed by a **Phase 1 foundation pass** (`cd6cf67`), a **Phase 2 live-path pass** (`889878f`+`253078a`), a **Phase 3 convergence/debt pass** (`1c32327`), and a **Phase 4 test-depth pass** (FOV-rotation regression, real CLI invocation tests, client-side tests, connect→act→observe→travel integration, the P1-10/P1-11 console-rendering fixes, and a `PhaseTimeout` fix that was intermittently failing real generations under load). Each audit carries its original **Reconciliation** banner (FIXED / PARTIAL / STANDS / NEW against the `d01fb94` state); this index and the per-subsystem banners have been updated further to reflect Phases 1–4. **The headline findings are now fixed**: the server boots with Orleans, the build is green, the live paths are validated and not trivially exploitable, the structural debt is cleared, and the coverage that was hollow (parser-only CLI tests, no client tests, no rotation regression, no travel test) is real. Suite at **928+38 / 0 skipped**. The remaining foundational gap is **CI (P2-1)** — deliberately deferred, a project cost decision — so there is still no automated gate; treat `dotnet build && dotnet test` as a manual pre-merge habit until that changes.

## Ground truth

| Check | Baseline `5b7e267` (2026-07-03) | `develop` @ `d01fb94` (2026-07-03) | `develop` @ `cd6cf67` — post-Phase-1 (2026-07-03) |
|---|---|---|---|
| `dotnet build Aetherium.sln` | **FAILS** — 11 errors, all in `Aetherium.Dashboard`: (a) `IClusterClient.Connect/Close` removed in Orleans 7+ (`Services/OrleansClientConnectionService.cs:29,59`); (b) a Razor **type-shadowing collision** — `Pages/BehaviorAnalysis.razor` binds the unqualified name `BehaviorAnalysis` to the component class rather than the server model (which *does* have those properties), affecting it and `AdaptationMonitor.razor`. 235 warnings | **STILL FAILS** — identical 11 Dashboard errors. 283 warnings | **PASSES — 0 errors** (Dashboard fixed: page renamed, dead Orleans service deleted). 281 warnings |
| `dotnet test Aetherium.Test` | **703 passed, 0 failed, 2 skipped** (6m23s) | **864 passed, 0 failed, 2 skipped** (3m28s) | **928 passed, 0 failed, 0 skipped** (~5m post-Phase-4) — both stale skips cleared; +64 Phase-1/2/3/4 tests |
| `dotnet test Aetherctl.Test` | **31 passed, 0 failed** | unchanged (31 passed) | **38 passed, 0 failed** — +7 real in-process invocation tests (the original 31 were parser-only) |
| Target framework / runtime | net9.0; machine has only the .NET 10 runtime, so tests need `DOTNET_ROLL_FORWARD=Major` | same | **Moot** — all 8 projects now target **net10.0**, matching the machine's SDK; no roll-forward needed |
| The skipped tests | `Add_And_List_Prompts` (PromptRegistryGrainTests) + 1 lighting-rendering, both with stale reasons | still 2 skipped | **0 skipped** — `PromptRegistryGrainTests` un-ignored (passes); the commented-out `LightingRenderingTests` corpse deleted |
| OpenSpec inventory | 20 capability specs, 7 active changes, 15 archived (`openspec` CLI not installed) | unchanged | unchanged |
| Server boot (Orleans **enabled**) | **HANGS at startup** (no banner after 90s — self-referential co-hosting DI bridge, `Program.cs:276-348`) | **STILL HANGS** (only the `IGrainFactory` self-ref was removed; `IClusterClient`/`IWorldHost` still self-resolved) | **BOOTS** — banner + `Started silo`, 0 exceptions (boot-tested, 40s healthy run). Fix needed *two* changes: deleting the whole self-referential DI bridge, and registering the `PubSubStore` grain storage that `AddMemoryStreams` requires (a second defect the hang had been masking) |
| Solution/project registration | Aetherctl builds only transitively (not in `.sln`) | unchanged | **Fixed** — `Aetherctl` added to `Aetherium.sln` (8/8 projects now listed) |

## Audit reports & scorecard

Maturity is a subjective roll-up: 🟢 solid, 🟡 works but has real gaps, 🟠 partially wired / significant issues, 🔴 broken or non-functional as shipped. The **Baseline** column is the audit as written (`5b7e267`); **develop** is the maturity after both the `d01fb94` reconciliation and the `cd6cf67` Phase-1 pass.

| Audit | Area | Baseline | develop | Reconciled top concerns |
|---|---|---|---|---|
| [orleans-and-hosting.md](orleans-and-hosting.md) | Silo, grains, persistence, DI | 🔴 | 🟡 | **Fixed:** server now boots with Orleans (DI bridge deleted, `PubSubStore`/`metaStore` registered), world persistence, driven tick pipeline, SQLite storage; **action-path convergence settled** (`ExecuteTool`+gateway is the one owner; P1-4 closed in Phase 3). **Still open:** tick pipeline doesn't drive NPC behavior (Phase-5 feature work) |
| [client-server-protocol.md](client-server-protocol.md) | Hubs, DTOs, auth, session lifecycle | 🟡 | 🟢 | *Fixed (Phases 2–3):* **movement validation**, **interaction reach**, **REST control-plane auth**, **legacy-session race serialization**, **action-path convergence** (obsolete hub methods gone; `ExecuteTool` now emits item narrative events too); plus reconnect, JoinWorld, ExecuteTool auth |
| [worldgen-and-pcg.md](worldgen-and-pcg.md) | Generator pipeline, prefabs, validation | 🟡 | 🟡 | Deep placement work still open (prefab-entity placement, generator params driving difficulty — P3-6). *Fixed (Phase 3):* **pass-list divergence gone** (shared `WorldGenerationPassCatalog`; server worlds now themed + populated with monsters/treasure/lighting), **`MapValidator` runs on live worlds**, typos; *earlier:* determinism, error handling, generator registry, prefab loading, A/B seed |
| [simulation-core.md](simulation-core.md) | ECS, interactions, clock/weather | 🟠 | 🟡 | NPCs still static; `WorldClock`/weather still not thread-safe (P1-13). *Fixed (Phase 2):* **movement now validated on all three paths** (`ObstructsMovement` was written but never read), **interaction reach rule**, **capacity exploit**, **`MoveEntity` index-drop**, **legacy-session races serialized**; plus West/East axis, driven ticks, `Memory.AddMemory` |
| [perception-fov-lighting.md](perception-fov-lighting.md) | FOV, lighting, vision modes | 🟢 | 🟢 | NPCs not drawn on the client. *Fixed (Phase 2):* **infrared renders** (heat carried in `LightLevel`; a latent `Component.Get<T>` crash also fixed); co-op observer perception; FOV rotation still fixed-by-design |
| [agents-and-tools.md](agents-and-tools.md) | Tool registry, profiles, agent grains | 🟡 | 🟡 | Runner still off-scheduler; agent↔game integration still open (P3-5). *Fixed (Phase 3):* **telemetry/replay growth capped**, **`ScheduleTransportTool` uses the real route** (was fabricated), **audio-repo stubs decline honestly**; *earlier:* hub auth bypass, tool-context Session, prompt copy-to-output |
| [narrative-and-multiworld.md](narrative-and-multiworld.md) | Narrative, portals, instances, meta | 🔴 | 🟡 | Quests can't complete (`ActiveQuestIds` unpopulated); ACL unenforced; instances/parties unreachable. *Fixed (Phase 3):* **item narrative events fire from `ExecuteTool`**, **generated-quest/lockout/invite growth capped**; *earlier:* travel, co-op, persistence, `metaStore`, deterministic narrative seed |
| [console-client.md](console-client.md) | Rendering, audio, input, monitoring | 🟡 | 🟢 | Remaining gaps are audio assets/monitoring polish. *Fixed (Phase 4):* **render lock (torn frames)**, **STATUS panel + markup escaping**, **manual-restart reconnect gap**, first real client-side unit tests; *(Phase 3):* ~6.3k lines of dead legacy engine deleted; *earlier:* reconnect soft-lock, dead input block |
| [unity-and-dashboard.md](unity-and-dashboard.md) | Unity client, Blazor dashboard | 🔴 | 🟢 | Unity: live-mode stub + tiles invisible (unchanged, tracked as P3). **Dashboard build fixed** (0 errors; page renamed, dead Orleans service removed) — remaining Dashboard gaps (port collision, stub pages, no tests) are non-blocking |
| [tooling-testing-devex.md](tooling-testing-devex.md) | aetherctl, WorldGenCLI, tests, CI | 🟡 | 🟡 | **Still no CI** (deliberately deferred, cost decision) — the one remaining foundational gap; TestCluster sharing (P2-7) also deliberately skipped. *Fixed (Phase 4):* **real CLI invocation tests** (+7; `Environment.Exit` removed from the error path, `AETHERCTL_CONFIG_DIR` override added), **client-side tests**, FOV-rotation regression, travel integration; *earlier:* +225 tests total (928+38, 0 skipped), OpenSpec `tasks.md` truthfulness, Orleans pin, Aetherctl in `.sln` |

**The structural finding from the original audit is now resolved.** The gates that kept a player from reaching the "island" subsystems are fixed (Phase 1: boot + build), the live paths are correct and not exploitable (Phase 2), and the structural debt is cleared (Phase 3): the dead duplicate console engine is deleted, one shared pass list means in-game worlds finally match the rich CLI previews (themed, populated with monsters and treasure), the action surface is a single `ExecuteTool`+gateway path, unbounded collections are capped, and the OpenSpec task docs tell the truth. What remains: **CI** (P2-1, deliberately deferred) as the missing safety net; the Phase-4 test-depth items; and the Phase-5 feature completions — starting with NPCs that visibly act (they now *exist* in worlds but don't tick). See [DESIGN_ANALYSIS.md](DESIGN_ANALYSIS.md) and [IMPROVEMENT_PLAN.md](IMPROVEMENT_PLAN.md) for what's next.
