# Aetherium Recommendations Register

*Date: 2026-07-03. A consolidated, de-duplicated, prioritized list of the actionable findings from the ten subsystem audits. Each item links to its source audit for `file:line` evidence. Severity reflects impact if the affected path is exercised; **Effort** is a rough order of magnitude (S ≈ hours, M ≈ a day or two, L ≈ a week+). "Quick win" = high value / low effort.*

Priority bands:
- **P0 — Correctness & security**: wrong behavior, exploits, or data-loss on paths that are (or should be) live.
- **P1 — Consistency & debt**: drift, dead code, and half-wired features that mislead or will bite soon.
- **P2 — Test & CI foundation**: the safety net that would have caught most P0/P1 items.
- **P3 — Feature completion**: finishing the scaffolded-but-unwired subsystems.

See [IMPROVEMENT_PLAN.md](IMPROVEMENT_PLAN.md) for the sequenced execution plan and [DESIGN_ANALYSIS.md](DESIGN_ANALYSIS.md) for the strategic framing.

> **Reconciliation status (`develop` @ 2026-07-04, updated after Phase 5).** Since this register was written (baseline `5b7e267`), six landing passes closed most of it: the 23-commit reconciliation (`d01fb94`), the **Phase-1 foundation pass** (`cd6cf67`), the **Phase-2 live-path pass** (`889878f` + `253078a`), the **Phase-3 convergence/debt pass** (`1c32327`), the **Phase-4 test-depth pass**, and now the **Phase-5 feature-completion pass**. **🟢 Done in Phase 5 (the P3 subsystem completions):** P3-2 (quest activation — `add-quest-activation`), P3-4 (instances/parties/raids lifecycle + entry surface — `add-instance-lifecycle-and-entry`), P3-5 (agent↔game live-map play), P3-6 (real PCG placement & difficulty — `add-pcg-placement-difficulty`), P3-7 (combat system, both slices — `add-combat-system`), P3-9 (console audio graceful fallback + assets — `complete-console-audio`), and P3-10 (dashboard stub pages + full decoupling of shared contracts into `Aetherium.Model.*` — `complete-dashboard-stub-pages` → `move-contracts-to-model` → `finish-dashboard-decoupling` → `renamespace-model-contracts`). **🟢 Done in Phase 4:** P2-4 (FOV rotation-invariance regression tests), P2-5 (real in-process CLI invocation tests + the `Environment.Exit` removal and `AETHERCTL_CONFIG_DIR` override that enabled them), P2-6 (client-side widget/markup tests via the extern alias; stale README deleted), P2-2 (connect→act→observe→**travel** integration test with two-way perception isolation), and the two console rendering debts the tests interacted with: P1-10 (render lock — torn frames) and P1-11 (STATUS panel + markup escaping; plus the `GameClient` manual-restart reconnect gap). Also fixed: the worldgen `PhaseTimeout` default was raised 2s→15s — it's a hung-pass safety net, and at 2s wall-clock it intermittently failed real generations and hydrations under load. **🟢 Done in Phase 3:** P1-4, P1-6, P1-8, P1-12, P1-14, P1-15, P1-20, P1-22. **🟢 Done in Phase 2:** P0-1, P0-3, P0-7, P0-8, P0-9, P0-10, P0-12. **🟢 Done earlier:** P0-0, P0-2, P0-4, P0-5, P0-6, P0-11, P1-1, P1-3, P1-5, P1-7, P1-9, P1-16, P1-17, P1-18, P1-19, P2-8, P2-9, P2-10, P3-1, P3-3, P3-8. **🟢 Also done 2026-07-04:** P1-13 (thread-safe `WorldClock`/`WeatherSystem`/`EventScheduler` + the `Tick`-elapsed and weather-cadence correctness fixes — `6f29a31`). **⬜ Still open:** **P1-2 is moot** (net10.0), P1-21 (script bugs), P2-7 (suite speed — investigated 2026-07-04 and deliberately not shipped: cluster-sharing is unsafe here and fixture parallelism gave only a marginal, noisy win while adding flakiness risk to the sole gate). **P2-1 (CI) is intentionally NOT implemented** — a build+test workflow was added (`e172f7d`) then **removed** (`cd34015`, 2026-07-04) because GitHub Actions cost is not currently affordable; local `dotnet build`+`dotnet test` remains the pre-merge gate. Items below are tagged inline: 🟢 done · 🟡 partial · ⬜ open. **The only open *work* left is P1-21 (script bugs); everything else is done or a deliberate non-implementation (P2-1 cost, P2-7 stability tradeoff).**

---

## P0 — Correctness & security

| # | Item | Sev | Effort | Source |
|---|---|---|---|---|
| P0-0 🟢 | **Delete the self-referential co-hosting DI bridge** — ~~the server cannot boot with Orleans enabled~~ **Done on develop** (`cd6cf67`): the whole bridge deleted (co-hosted grains already share the host container). This surfaced a second, previously-masked defect — `AddMemoryStreams("Default")` requires a `PubSubStore` grain storage that was never registered — also fixed in the same commit. Boot-verified: startup banner + `Started silo`, 0 exceptions. `launchSettings.json` URL drift (50309/50310 vs documented 5000) is unaddressed and low-priority. | Critical | S (quick win) | [orleans](orleans-and-hosting.md) |
| P0-1 🟢 | **Validate player movement server-side** — **Done on develop** (`889878f`): new engine core `World.MovementBlocker` (destination exists + terrain passable + no `ObstructsMovement` entity + no other character) with per-step `TryMoveSteps` and stair-gated `TryChangeLevel`. All three live paths route through it — `GameSession.MoveView/ChangeLevel`, `GameMapGrain.MoveAsync/ChangeLevelAsync` (now also clamps distance to 1–100), and `GameManagementGrain`. Root cause was stark: `ObstructsMovement` was written by walls/doors/windows but **never read**, so nothing blocked movement anywhere. (Bonus: aligned the dungeon generator's previously-untraversable stair pairs.) The three *action paths* aren't yet collapsed to one owner — that's P1-4. | Critical | M | [protocol](client-server-protocol.md), [simulation](simulation-core.md) |
| P0-2 🟢 | **Fix `World.TryMove` West/East axis swap** — ~~West/East use North/South deltas~~ **Done on develop** (`World.cs:287/289` now correct); note `TryMove` is still dead code, so this is a landmine removed rather than a behavior change. | Critical | S (quick win) | [simulation](simulation-core.md) |
| P0-3 🟢 | **Range-check door/use interactions** — **Done on develop** (`889878f`): a shared reach rule (target at/cardinally-adjacent, same Z) now guards open/close, direct `unlock-door`/lockpick/force-open (the `TryUseWithMode` path that bypasses option-context filtering), and `TryActivate` (which gained the missing Z check). Lever mechanisms still act on remote linked doors — only the lever itself is reach-checked. | High | S | [protocol](client-server-protocol.md), [simulation](simulation-core.md) |
| P0-4 🟢 | **Register `metaStore` (and audit all `[PersistentState]` store names)** — ~~`MetaProgressionGrain` throws on every production activation~~ **Done on develop** (`cd6cf67`): registered alongside the newly-discovered `PubSubStore` requirement (P0-0). (The related `ORLEANS_STORAGE≠memory` silent-no-storage case *was* fixed via `ResolveStorageConfiguration`.) | Critical | S (quick win) | [narrative](narrative-and-multiworld.md), [orleans](orleans-and-hosting.md) |
| P0-5 🟢 | **Fix `Memory.AddMemory`** — ~~writes to a discarded list copy~~ **Done on develop** (`cd6cf67`, both the Server and Console copies): now mutates the stored list directly, matching `RemoveMemory`. | Critical | S (quick win) | [simulation](simulation-core.md) |
| P0-6 🟢 | **Centralize tool authorization at the hub** — ~~`GameHub.ExecuteTool` never calls `IsToolAllowed`~~ **Done on develop**: the hub now checks `AgentToolProfile.Player.IsToolAllowed(tool)` before executing. | High | S | [agents](agents-and-tools.md), [protocol](client-server-protocol.md) |
| P0-7 🟢 | **Add auth to the REST control-plane** — **Done on develop** (`889878f`): `ApiKeyMiddleware` now also gates mutating requests (POST/PUT/PATCH/DELETE) to `/api/adaptation`, `/api/benchmark`, `/api/cluster`, `/api/curriculum`, `/api/metaprogression`, `/api/agenttelemetry` (previously fully anonymous — economy ticks, rule reloads, curriculum file writes). Reads stay open for the Dashboard's keyless monitoring pages; `/api/management` remains fully protected. | High | M | [protocol](client-server-protocol.md) |
| P0-8 🟢 | **Fix infrared rendering** — **Done on develop** (`889878f`): `ComputeHeatVision` records each heated cell's intensity into the light frame, so `VisualDto.LightLevel` (the heat channel infrared clients color by) carries real heat instead of 0.0. The regression test also exposed and fixed a latent crash — `InfraredVisionSystem` used `Component.Get<T>()`, which throws when absent, so infrared perception crashed on any entity without a heat signature. | High | S | [perception](perception-fov-lighting.md) |
| P0-9 🟢 | **Close inventory-capacity exploit** — **Done on develop** (`253078a`): `CapacityBoost` gained an `IsEquipped` flag; `TryEquip` applies the bonus once and refuses re-equipping the same item, so repeated equips can't grant unbounded capacity. | High | S | [simulation](simulation-core.md) |
| P0-10 🟢 | **Harden `RemoveEntity`/`MoveEntity`** — **Done on develop**: `RemoveEntity`'s throw-before-null-check and the concurrent-pickup dupe were fixed via the atomic `TryRemoveEntity` (reconciliation era); the remaining `MoveEntity` index-drop edge is closed in `253078a` (the destination write is now the idempotent indexer, so the entity is always indexed at exactly one place even under a concurrent double-move). | High | M | [simulation](simulation-core.md) |
| P0-11 🟢 | **Fix the console reconnect soft-lock** — ~~after a full reconnect, input is ignored forever~~ **Done on develop**: `GameClient` re-raises `Connected` on the auto-`Reconnected` path. (Minor: the loop still doesn't re-render a "Reconnecting…" state during downtime.) | High | S | [console](console-client.md) |
| P0-12 🟢 | **Cross-path state races** — **Done on develop** (`253078a`): `GameSession.WithStateLock` makes `_stateLock` the single serialization point for *all* legacy-session mutation. Movement/rotation/level-change/perception already took it; interactions (pickup/drop/use/open/close) now route through it from both `LocalMutationGateway` (SignalR) and `GameManagementGrain` (silo), so the two can no longer interleave mutations of the same World/Player. Grain-bound sessions stay serialized by Orleans. (Full collapse of the three action paths onto one owner remains P1-4.) | Medium | M | [protocol](client-server-protocol.md), [simulation](simulation-core.md) |

## P1 — Consistency & technical debt

| # | Item | Sev | Effort | Source |
|---|---|---|---|---|
| P1-1 🟢 | **Fix the Dashboard build (2 root causes)** — **Done on develop** (`cd6cf67`): renamed `BehaviorAnalysis.razor` → `BehaviorAnalysisPage.razor` (kills the type-shadowing collision affecting it and `AdaptationMonitor.razor`); deleted `OrleansClientConnectionService` and rely on the existing `UseOrleansClient`. Full-solution compile restored (0 errors). | High | S (quick win) | [unity+dashboard](unity-and-dashboard.md), [agents](agents-and-tools.md) |
| P1-2 🟢 | **Resolve the .NET-9 runtime mismatch** — **Moot on develop**: all 8 projects migrated to net10.0, matching the dev machine's SDK. No `global.json`/`RollForward` needed. | High | S (quick win) | [tooling](tooling-testing-devex.md) |
| P1-3 🟢 | **Add Aetherctl to `Aetherium.sln`** — **Done on develop** (`cd6cf67`): all 8 projects on disk are now listed in the solution. | Medium | S (quick win) | [tooling](tooling-testing-devex.md) |
| P1-4 🟢 | **Converge the action paths** — **Done** (mostly pre-existing, register was stale): the per-verb `[Obsolete]` hub methods were removed in `9d2e150` (part of the grain-authoritative multiworld work) — `ExecuteTool` + `IMapMutationGateway` (Local/Grain) is the only action surface, and `client-server-communication/spec.md` already mandates it. Phase 3 (`1c32327`) closed the remaining parity gap: `ExecuteTool` now emits `item_collected`/`item_used` narrative events (door events already fired), so the tool path matches everything the retired methods did. | Medium | M | [protocol](client-server-protocol.md), [agents](agents-and-tools.md) |
| P1-5 🟢 | **Populate the server's `MapGeneratorRegistry`** — **Done on develop** (`cd6cf67`): `DiscoverTypes` now runs at construction, so the game server honors the requested generator instead of always falling back to `AdvancedDungeonGenerator`. | High | S (quick win) | [worldgen](worldgen-and-pcg.md) |
| P1-6 🟢 | **Unify the worldgen pass lists** — **Done in Phase 3** (`1c32327`): new `WorldGenerationPassCatalog` is the single pass-list source for `GameMapGrain`, `SnapshotWorldBuilder`, WorldGenCLI, and aetherctl. Server maps now run theming/population/environmental-story/audio (previously CLI-only), so in-game worlds contain monsters, treasure, and ambient lighting. Unification surfaced and fixed two defects: snapshot hydration would have duplicated population monsters (Characters weren't stripped before overlay), and spawn selection could place a joiner inside a monster (new `World.IsOpenForOccupancy`). | High | M | [worldgen](worldgen-and-pcg.md) |
| P1-7 🟢 | **Wire prefab loading** — **Done on develop** (`cd6cf67`): the server now calls `PrefabLibrary.LoadFromDirectory` in file-storage mode; `Data/Prefabs/**/*.json` load at startup. | Medium | S (quick win) | [worldgen](worldgen-and-pcg.md) |
| P1-8 🟢 | **Delete dead legacy client code** — **Done in Phase 3** (`1c32327`): `ConsoleDungeonGame`, `ClientConsoleDungeonGame`, and the duplicate client-side engine deleted (~6.3k lines; Components/Entities/Geometry/Lighting/Perception/WorldBuilders directories plus the legacy `ConsoleMapView` and Core files). `Enums.cs` trimmed to the live `WorldDirection`/`RelativeDirection`; `Extensions.cs` to the live `Point`/`Size` `FromDelta` overloads. The proposed "extract a shared engine library" follow-up is moot — the duplicate was dead, not shared. | Medium | M | [console](console-client.md) |
| P1-9 🟢 | **Fix the dead console input block** — **Done on develop** (`cd6cf67`): the compass-mode/next-track logic (stranded after a `break;` following the Mode-4 case) moved into the `ConsoleKey.M` case; Shift+M and M now work as the help panel advertises. | Medium | S (quick win) | [console](console-client.md) |
| P1-10 🟢 | **Synchronize console rendering** — **Done in Phase 4**: `RenderCurrentState` now serializes whole frames behind a render lock, so a perception update landing on a SignalR thread can no longer interleave console writes with the input loop's render and tear the frame. | Medium | S | [console](console-client.md) |
| P1-11 🟢 | **Render `StatusMessage` and fix inventory markup** — **Done in Phase 4**: the renderer gained a STATUS panel (feedback was previously invisible — `StatusMessage` was set on every action but never drawn), and inventory/status text now flows through escaped markup builders (`BuildInventoryItemsMarkup`/`BuildStatusMarkup`), so a key id like `[gold-key]` renders as text instead of crashing the frame as an unknown Spectre tag. Covered by client-side unit tests (P2-6). Also fixed in passing: `GameClient`'s last-resort `Closed`-handler restart now re-raises `Connected` (the auto-reconnect path already did — a successful manual restart used to leave input soft-locked) and its retry delay can no longer be 0 ms. | Medium | S | [console](console-client.md) |
| P1-12 🟢 | **Decide `WorldTickService`'s fate** — **Decided & documented (Phase 3)**: it stays — it's a real, documented `BackgroundService` that enumerates worlds at `SimulationOptions.TickHz` and drives `IWorldGrain.TickAsync` (weather/season/region ticks run, with backpressure protection). NPC behavior ticking (`Monster.Heartbeat` invocation, so monsters act) is *feature* work, tracked under Phase 5 slice 1 (P3-1's "NPCs visible and ticking"), not debt. | Medium | M | [simulation](simulation-core.md), [orleans](orleans-and-hosting.md) |
| P1-13 🟢 | **Make `WorldClock`/`WeatherSystem`/`EventScheduler` thread-safe** — **Done 2026-07-04 (`make-simulation-services-thread-safe`, merged `6f29a31`)**: all three singletons now guard their mutable state with a lock (`EventScheduler` snapshots due events under the lock and awaits handlers outside it; `WeatherSystem` uses thread-safe `Random.Shared`). Also fixed the two correctness bugs: `WorldClock.Tick()` now returns the game time since the previous `Tick()` regardless of interleaved reads (via `_accumulatedAtLastTick`), and weather transition probability scales with elapsed game hours so cadence follows game time, not call count. `SeasonManager` was already stateless. Covered by `SimulationThreadSafetyTests` (concurrency stress + regressions). | Medium | M | [simulation](simulation-core.md) |
| P1-14 🟢 | **Remove/implement `NotImplementedException` & stub repos** — **Done in Phase 3** (`1c32327`): the SQL Server/Cosmos audio repos now throw `NotSupportedException` with a clear "use JsonAudioProfileRepository" message (they're unreachable from live code); `ScheduleTransportTool` now fetches the real trade route from the cluster's economy state and returns an honest error when it doesn't exist (it previously scheduled against a fabricated route with only `RouteId` set). `SpawnEntityTool`/`ModifyEntityTool` were verified to already return honest tool errors — no change needed. | Medium | M | [agents](agents-and-tools.md), [console](console-client.md) |
| P1-15 🟢 | **Cap unbounded growth** — **Done in Phase 3** (`1c32327`): telemetry snapshots capped at 1000/agent (most-recent kept); `ReplayStorage` (the worst offender — process-lifetime static, MBs/entry) capped at 200 per store with FIFO eviction; generated quests capped at 500; expired lockout-ledger entries purged after 7 days (attempt-locked entries retained); expired/stale-accepted invites purged on create. Failed-run IDs and narrative events were already capped. | Medium | M | [agents](agents-and-tools.md), [narrative](narrative-and-multiworld.md) |
| P1-16 🟢 | **Fix the A/B worldgen seed bug** — **Done on develop** (`cd6cf67`): auto-seeded candidates now vary by range index instead of all sharing `Environment.TickCount`. | Medium | S (quick win) | [worldgen](worldgen-and-pcg.md) |
| P1-17 🟢 | **Deterministic narrative seeds** — **Done on develop** (`cd6cf67`): replaced `string.GetHashCode()` (randomized per process) with a stable FNV-1a hash. | Medium | S (quick win) | [narrative](narrative-and-multiworld.md) |
| P1-18 🟢 | **Copy `Prompts/*.md` to output** — **Done on develop** (`cd6cf67`): `CopyToOutputDirectory` item added; `PromptRegistry` no longer reads an empty bin dir. | Low | S (quick win) | [agents](agents-and-tools.md) |
| P1-19 🟢 | **Align Aetherctl's Orleans package pin** — **Done on develop**: now references `Microsoft.Orleans.Client 9.2.1`, matching the server. | Low | S (quick win) | [tooling](tooling-testing-devex.md) |
| P1-20 🟢 | **Reconcile OpenSpec `tasks.md` with reality** — **Done in Phase 3** (`1c32327`): all 8 active changes with a `tasks.md` verified against code and updated with dated status notes. Biggest drift: `add-unity-2d-client` and `add-worldbuilding-tool-integration` were mostly done but all-unchecked; `add-agent-training-pcg` claimed a nonexistent `train` subcommand; `add-multiworld-ecosystems` claimed quest flow that can't complete (`ActiveQuestIds` never populated). Note: `add-multi-use-tools` has no `tasks.md` at all. | Medium | M | all audits |
| P1-21 | **Fix script bugs** — `monitor-lite.ps1` decodes the wrong byte count (frames never parse); `start-llm-agents.ps1` references the nonexistent `agentcli`; Ctrl+C handlers use `$using:` incorrectly and track wrapper PIDs. | Low | S | [tooling](tooling-testing-devex.md) |
| P1-22 🟢 | **Enforce or drop `MapValidator` at runtime** — **Done in Phase 3** (`1c32327`): `GameMapGrain` now runs `MapValidator` on every generated world (terrain-type registration check; diagnostic logging rather than gating, since the pipeline's own Validation pass already gates generation). "Argentinaable" typo and the mojibake comment fixed. | Low | S | [worldgen](worldgen-and-pcg.md) |

## P2 — Test & CI foundation

| # | Item | Sev | Effort | Source |
|---|---|---|---|---|
| P2-1 ⬜ | **Stand up CI** — **Intentionally NOT implemented (project cost decision).** A `windows-latest` build+test workflow was added (`add-ci-build-test-gate`, `e172f7d`) then **removed** (`cd34015`, 2026-07-04) because GitHub Actions minutes — especially the 2× Windows-runner multiplier the suite needs — cost more than the project can currently afford. Do NOT re-create it without an explicit new cost go-ahead. The pre-merge gate stays a manual local `dotnet build Aetherium.sln && dotnet test Aetherium.Test`. (Note: the pre-existing `deploy-server.yml` Azure-deploy workflow on `master` push is untouched.) | High | S (quick win) | [tooling](tooling-testing-devex.md) |
| P2-2 🟢 | **Integration tests across grain boundaries** — **Done to its completable extent (Phase 4)**: `EndToEndSharedMutationTests` now covers the full connect→act→observe→**travel** path — two players share a world and see each other act, one resolves a portal through the cluster grain, transports to a second world, and the perception fan-out isolates correctly in both directions (world-1 actions no longer reach the traveler; world-2 actions don't reach the stayer). Quest and instance legs remain excluded because those features can't complete (P3-2/P3-4) — extend the test when they land. | High | L | [narrative](narrative-and-multiworld.md), [protocol](client-server-protocol.md) |
| P2-3 🟢 | **Cover the real player-movement path** — **Done on develop** (`889878f`): `MovementValidationTests` covers passability, map-edge, closed/open doors, occupancy, partial multi-step moves, and stair-gated level changes across `World.TryMoveSteps`/`TryChangeLevel` and the `GameSession` entry points; `Phase2HardeningTests` adds the concurrency/index cases. | High | M | [simulation](simulation-core.md) |
| P2-4 🟢 | **FOV rotation-invariance regression test** — **Done in Phase 4**: `FovRotationInvarianceTests` sweeps cardinal and non-cardinal headings asserting the omnidirectional visible set (relative keys AND per-key terrain) never changes, the directional cone is a strict rotating subset of the omni set, and toggling directional mode off restores the identical set at any heading. The historical bug class can't silently return. | Medium | S | [perception](perception-fov-lighting.md) |
| P2-5 🟢 | **Real CLI tests** — **Done in Phase 4**: `CommandInvocationTests` runs actual handlers in-process (via a new `Program.BuildRootCommand`) asserting stdout/stderr and exit codes: worldgen generation (text + parseable `--json`), the full server add→list→connect→remove config round-trip (isolated via a new `AETHERCTL_CONFIG_DIR` override), error and parse-error exit codes, help. Enabler fix: `Common.WriteError` called `Environment.Exit(1)` — killing the process before `await using` disposal and making in-process tests impossible; it now records an exit code that Main returns. Network-bound commands (login/status/session) still need a live server and remain untested offline. | Medium | M | [tooling](tooling-testing-devex.md) |
| P2-6 🟢 | **Client-side unit tests** — **Done in Phase 4** via the existing extern-alias approach (no separate project needed): `ClientWidgetAndMarkupTests` covers CompassWidget (visibility, heading/direction data, mode cycling), InventoryWidget (formatting, visibility), and the P1-11 markup-escape builders (bracketed key ids and injected style tags parse safely). The stale `CLIENT_TESTS_README.md` templates are deleted. Deeper `GameClient`/renderer integration tests still need a `HubConnection` factory / `IAnsiConsole` injection refactor. | Medium | M | [console](console-client.md), [tooling](tooling-testing-devex.md) |
| P2-7 ⬜ | **Speed up the suite** — **Investigated 2026-07-04; deliberately not shipped.** Two levers were evaluated: (a) *sharing* TestClusters is largely unsafe here — the ~26 fixtures' silo configs are genuinely fragmented (SignalR stubs, per-fixture `IWorldSnapshotStore`/recording-store instances the tests assert against, distinct `PersistenceOptions`), and several capture per-fixture instance state, so only ~3 fixtures share an identical stateless config; (b) *fixture-level parallelism* (NUnit `ParallelScope.Fixtures`) was prototyped and ran green 3× but yielded only a marginal, noisy speedup (~4m vs ~4m23s) because the wall-clock floor is set by a handful of long serial integration fixtures, **not** by cluster-deploy concurrency — widening past 4-way didn't help. Given CI is off, the local suite is the sole gate, so adding a known Orleans-TestCluster flakiness vector for a ~10% win was judged not worth it (consistent with this item's original Low/skip call). The real lever, if revisited, is profiling and optimizing the 2–3 slowest integration fixtures directly. | Low | M | [tooling](tooling-testing-devex.md) |
| P2-8 🟢 | **Un-skip / delete the 2 stale skipped tests** — **Done on develop** (`cd6cf67`): `PromptRegistryGrainTests` un-ignored (passes); `LightingRenderingTests`' commented-out corpse deleted. 0 skipped tests remain. | Low | S (quick win) | [tooling](tooling-testing-devex.md) |
| P2-9 🟢 | **Byte/tile-level determinism test for PCG** — **Done on develop**: `Aetherium.Test/WorldGen/DeterminismTests.cs` now hashes the full world (same seed → same hash) and verifies `EffectiveSeed` replay. | Low | M | [worldgen](worldgen-and-pcg.md) |
| P2-10 🟢 | **Fix the Unity project so it imports & tests** — **Done on develop** (Unity 6 migration: `Packages/manifest.json`, asmdefs in subfolders, `ProjectSettings`, `Main.unity`, PlayMode tests compile). Remaining is content/behavior (live-mode client, tile sprites), tracked under P3. | Medium | M | [unity+dashboard](unity-and-dashboard.md) |

## P3 — Feature completion (scaffolded but unwired)

Each of these is a subsystem the docs present as built but which does not function end-to-end. Complete or explicitly re-scope. Most warrant an OpenSpec change (or reconciliation of an existing one) before work starts.

| # | Item | Sev | Effort | Source |
|---|---|---|---|---|
| P3-1 🟢 | **Push perception to observers** — **Done for grain-hosted maps on develop** (delta fan-out + per-session FOV recompute; co-located players see each other move). **Phase 5:** NPC movement now emits too — `GameMapGrain.TickAsync` fans out monster moves and the client renders them (`VisibleCharacters`), so co-located players see monsters wander. Remaining: legacy single-world sessions still get no observer updates. | High | M | [perception](perception-fov-lighting.md) |
| P3-2 🟢 | **Wire the narrative consequence engine into gameplay** — **Done (Phase 5, `add-quest-activation`)**: `item_collected`/`item_used` now emit (P1-4), and quest activation is wired end-to-end — `ActiveQuestIds` is populated via grain activation, and `collect`/`kill`/`reach_location` objectives complete (combat's `enemy_defeated` feeds the `kill` objective — P3-7). Player/agent/CLI surfaces can accept and progress quests. | High | L | [narrative](narrative-and-multiworld.md) |
| P3-3 🟢 | **Make multi-world travel work end-to-end** — **Done on develop**: `JoinWorld` binds a session to a grain world (snapshot hydration + gateway swap) and `UsePortal` resolves a target, transports the player, and emits `player_arrived`. (Follow-ups: ACL still unenforced at join; world metadata/tags still unpopulated so tag-portals fall back to first-world; join/fan-out races noted.) | High | L | [narrative](narrative-and-multiworld.md) |
| P3-4 🟢 | **Wire the instances/parties/raids stack** — **Done (Phase 5, `add-instance-lifecycle-and-entry`)**: added the hub/tool/CLI entry surface, lockout record-on-reuse, and a cleanup sweeper that reaps abandoned instances (they were previously leaked and ticked forever). | Medium | L | [narrative](narrative-and-multiworld.md) |
| P3-5 🟢 | **Finish agent↔game integration** — **Done on develop (Phase 5):** standardized on `AgentRunnerGrain`; `AttachToWorldAsync` joins an agent to a live map as a Character and acts through the map grain, so it plays the *shared* world visibly to players (perception via `GameMapGrain.ComputeAgentPerceptionAsync`); the runner loop moved onto an Orleans grain timer; `aetherctl agent attach-world` makes it reachable; the heuristic policy plays with no LLM, integration-tested. Follow-up: feed prompt templates into LLM decisions; per-profile attach; LLM decision quality. | High | L | [agents](agents-and-tools.md) |
| P3-6 🟢 | **Make PCG placement real** — **Done (Phase 5, `add-pcg-placement-difficulty`)**: NPC/item/prefab-entity placement now actually spawns entities (previously console-log stubs), and generator/difficulty parameters affect output instead of being decorative. | High | L | [worldgen](worldgen-and-pcg.md) |
| P3-7 🟢 | **Implement a combat system** — **Done, both slices (Phase 5, `add-combat-system`)**: slice 1 = attack/damage/death with `enemy_defeated` wiring the P3-2 `kill` quest loop; slice 2 = variable damage (`AttackPower` + best carried `Weapon`), monster retaliation/aggro on the tick (a 0-HP player is "downed" not deleted mid-tick), death loot (`SwordItem` drop), and per-map analytics (`GetCombatStatsAsync`/`CombatStatsDto` + `aetherctl combat stats`). | Medium | L | [simulation](simulation-core.md) |
| P3-8 🟢 | **Durable persistence** — **Largely done on develop**: `GameMapGrain` snapshots the `World` and restores it via `SnapshotWorldBuilder` + `MapDeltaReplayer`; a SQLite grain-storage/snapshot store, periodic + threshold compaction, and delta-version guards landed with solid tests. Follow-ups: heat state not persisted (cold-start loses trails); `PersistDeltaAsync` swallows failures; SQLite writer-concurrency and delta-GC under-tested. (Correction: an earlier follow-up note here claimed `metaStore` was "still unregistered" — it **is** registered, see `Aetherium.Server/Program.cs` `grainStoreNames`; that was fixed under P0-4.) | High | L | [orleans](orleans-and-hosting.md), [narrative](narrative-and-multiworld.md) |
| P3-9 🟢 | **Complete or scope down audio** — **Done (Phase 5, `complete-console-audio`)**: `NAudioSystem` gracefully falls back to silence when no output is available (`IsOutputAvailable()`; runtime device errors self-mute via `HandlePlaybackError`+`LastError` instead of corrupting the Spectre TUI); `AssetRoot` resolves under `AppContext.BaseDirectory` and the `.csproj` copies `Assets/Audio/**` to output; reverb/stereo-panning documented as unsupported (dead pan compute removed). | Low | M | [console](console-client.md) |
| P3-10 🟢 | **Complete the Dashboard** — **Done (Phase 5)**: the three stub pages (BenchmarkComparison, CurriculumProgress, ReplayViewer) now consume real data (`complete-dashboard-stub-pages`), and the shared contracts were fully relocated into `Aetherium.Model.*` across two passes (`move-contracts-to-model` → `finish-dashboard-decoupling`) then re-namespaced (`renamespace-model-contracts`). The Dashboard now references **only** `Aetherium.Model` — the `Aetherium.Server` and `WorldGenCLI` project references are gone. | Low | M | [unity+dashboard](unity-and-dashboard.md) |

---

## Quick-wins shortlist (high value, ≤ a few hours each)

**Still open on `develop`:** none. **P2-1 (CI)** is intentionally not implemented — a workflow was added then removed for cost (`cd34015`, 2026-07-04) — not a pending quick win.

**Landed in the Phase-1 foundation pass** (`cd6cf67`, 2026-07-03): P0-0 (boot-blocking DI bridge + the `PubSubStore` defect it was masking), P0-4 (metaStore), P0-5 (Memory), P1-1 (Dashboard build), P1-3 (Aetherctl in .sln), P1-5 (generator registry), P1-7 (prefab loading), P1-9 (dead input block), P1-16 (A/B seed), P1-17 (narrative seed), P1-18 (prompt copy), P2-8 (stale skips). P1-2 (runtime mismatch) turned out to be moot — `develop` had already migrated to net10.0.

**Landed earlier, in the 23-commit reconciliation** (`d01fb94`): 🟢 P0-2 (axis swap) · P0-6 (hub auth) · P0-11 (reconnect) · P1-19 (Orleans pin) · P2-9 (determinism test) · P2-10 (Unity imports).

The shortlist's exit criteria are met, and **Phases 2–5 have since landed too** (`889878f`+`253078a`, `1c32327`, the Phase-4 test-depth pass, and the Phase-5 feature-completion pass): the live paths are validated and not exploitable, the dead legacy console engine is gone, the worldgen pass lists are unified (server worlds are now populated), the stub tools decline honestly, the unbounded collections are capped, the OpenSpec `tasks.md` files tell the truth, and **every P3 subsystem is now complete** — quests, instances/parties/raids, agent live-map play, real PCG placement, combat, audio, and the Dashboard (fully decoupled onto `Aetherium.Model`). Suite at **~1027 / 0 skipped**. What actually remains: **P1-21** (script bugs) is the only open *work* (P1-13 simulation thread-safety was completed 2026-07-04, `6f29a31`); **P2-7** (suite speed) is a deliberate Low-priority skip — investigated and not shipped because the achievable win is marginal and parallelism would add flakiness risk to the sole gate; and **P2-1 (CI)** is intentionally not implemented — a workflow was added then removed for cost, so `dotnet build Aetherium.sln && dotnet test Aetherium.Test` stays the manual pre-merge gate. Without CI, nothing automated prevents a future commit from silently reintroducing a regression the way the boot hang and build break went unnoticed for months; stand it up whenever the cost tradeoff changes.
