# Unreal Sample (placeholder)

No Unreal sample exists yet. When one is built it lives here, following the same shape as the Unity sample: a presentation-only client for a `Data/Games/` bundle, connecting through the shared protocol.

Start with the [Unreal client migration guide](../../docs/clients/unreal-client-guide.md) — the reusable C# client layer it assumes is `Aetherium.Client` (see the [design suite](../../docs/design/unity-sample/README.md)).
